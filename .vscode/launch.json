{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Huidig bestand",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/lib"
      }
    }
  ]
}